syntax = "proto3";
package api;
option go_package = "example.com/go-grpc/api";

service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
}

message HelloRequest { string name = 1; }
message HelloReply { string message = 1; }
